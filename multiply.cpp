#include<iostream>

using namespace std;

int main()
{
    int a,b,result;
    cout<<"Enter two numbers :";
    cin>>a>>b;
    result = a* b;
    cout<<"Multiple of "<<a<<" & "<<b<<" is "<<result;

    return 0;
}